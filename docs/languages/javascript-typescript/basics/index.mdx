---
sidebar_position: 1
---

# JavaScript 言語をはじめよう

この節では、まず **入門編** で書き方を一通り押さえ、**値と参照・実行の仕組み・非同期** を掴んだうえで、言語の骨格へ進みます。

:::info[読み方]

入門編は、変数・制御構文・関数・データ構造・モジュールなど **入門に必要な書き方を一通り網羅** します。  
たとえ話多めで噛み砕いているので、手が動く人は流し読みで構いません。不安なところだけ戻っても大丈夫です。  
はじめて近い人は、下のロードマップ **1〜16**（入門編）を先に通してください。  
17 以降は、入門編を通し終えてからで構いません。

:::

## この節で分かること

- 小さなプログラムを書いて動かす書き方の入口
- 等価比較、クラス、例外、Map / Set、現代的な構文
- JavaScript を実行する仕組み（エンジン）で、同期の処理と「あとで動く」処理がどう並ぶか
- プリミティブとオブジェクト参照、Promise / `async` / `await`

## なぜ言語と実行をひと続きにするか

現場で最初につまずきやすいのは、構文そのものより次のような場面です。

- `undefined is not a function` の「何が undefined だったのか」が追えない
- 渡したオブジェクトの中身が、呼び出し元まで書き換わる
- `await` なしで Promise を触り、画面やログが「まだ途中」のまま進む
- フレームワークから呼ばれる関数や型が、何を受け取るか想像できない

値と参照、コールスタック、イベントループは別章にすると、障害のときに横断できません。  
ここで一度つなげておくと、あとから読む TypeScript や npm が「設定の暗記」になりにくいです。

:::tip[この節のゴール]

小さなスクリプトを動かし、型・参照・非同期・例外の会話についていけ、現代的な構文の選択理由を説明できる状態を目指します。

:::

## 学習のロードマップ

次の順で読むと、書き方を覚えたあと、そのコードがどの順番で動くかまで理解を積み上げられます。

**入門編**

1. [最初のプログラム](./first-program) … Node.js、`console.log`、コメント、エラーの読み方、`process.argv`
2. [変数と計算](./variables-and-calculation) … `let` / `const`、型の感覚、四則演算、比較
3. [文字列とテキスト](./strings-and-text) … 文字列操作、数値変換、`Math`、引数の扱い
4. [条件分岐](./branching) … `if` / `else`、三項演算子、`switch`、条件の組み合わせ
5. [等価と比較](./equality-and-coercion) … `===` / `==`、truthy / falsy
6. [繰り返し](./loops) … `for` / `while`、`break` / `continue`
7. [関数](./functions) … 定義と呼び出し、コールバック、矢印関数、早期 `return`
8. [スコープと変数の有効範囲](./scope-and-variables) … ブロックスコープ、関数スコープ、シャドーイング
9. [オブジェクト](./objects) … プロパティ、メソッド、分割代入の入口
10. [配列](./arrays) … 添字、破壊的 / 非破壊、`for...of`、`forEach`、`map` / `filter` / `reduce` など
11. [JSON とデータのやりとり](./json-and-data-shapes) … `JSON.stringify` / `JSON.parse`
12. [モジュール](./modules) … `import` / `export`、ファイル分割
13. [クラス](./classes) … `class`、コンストラクタ、継承の入口
14. [例外](./errors) … `try` / `catch`、`throw`、エラーの伝播
15. [Map / Set](./map-set) … 一覧とキー付きデータの選び方
16. [現代的な構文](./modern-syntax) … 分割代入、スプレッド、`?.` / `??`

**実行・参照・非同期**

17. [実行の仕組み](./execution-model) … エンジン、コールスタック、イベントループの入口
18. [型と参照](./types-and-references) … プリミティブとオブジェクト、コピーと共有
19. [Promise](./promises) … 非同期の約束、`then` / `catch`、合成
20. [`async` / `await` と fetch](./async-await-fetch) … 読みやすい非同期、HTTP の入口

各ページでは、概念の区切りごとに **理解チェック**（選択式／穴埋め）を挟みます。  
「一つ読んで → 短く確認 → 次へ」のリズムです。これらの問題はテストではなく、その場で理解が正しいかを確認するためのものです。

各ページのコード例は、**ES2022 以降** と **Node.js 20 以降** を基準にしています。  
題材は架空の EC サイト（商品・注文・在庫）でそろえています。  
ブラウザでも同じ構文の多くは動きますが、実行手順の本線はターミナルの Node.js です。  
利用する環境のバージョンが異なる場合は、構文が使えるか公式ドキュメントで確認してください。

先に [JavaScript / TypeScript をはじめよう](../) を読んでおくと、章全体の位置づけがつかみやすいです。  
この節のあとは [TypeScript](../typescript/) へ進みます。
