---
sidebar_position: 4
---

# 条件分岐

同じプログラムでも、状況によって動きを変えたいことがあります。  
「1000 円以上ならまとめ買い」「そうでなければ通常」——道を選ぶ書き方が **条件分岐** です。

[変数と計算](./variables-and-calculation) の比較（`>=` など）が、ここで材料になります。

## このページで分かること

- `if` / `else` でふたつの道を選ぶ
- `else if` で枝を増やす
- 条件は `boolean`（はい／いいえ）になる式であること
- `&&` / `||` / `!` で条件を組み合わせる入口

## `if` / `else`: 改札のふたつの道

駅の改札で、「切符が有効なら通る／だめなら別の案内」と同じです。

```java
int yen = 1200;

if (yen >= 1000) {
    System.out.println("bulk");
} else {
    System.out.println("normal");
}
```

読み方です。

1. `yen >= 1000` を調べる（結果は `true` か `false`）
2. `true` なら最初の `{ ... }` へ
3. `false` なら `else` の `{ ... }` へ

`else` は省略できます。  
「条件がはいのときだけ動かす」書き方です。

```java
if (yen >= 1000) {
    System.out.println("bulk");
}
```

:::tip[覚えておくこと]

`if` の丸括弧の中は、はい／いいえになる式。  
波括弧 `{ }` の中が、その道で実行する文。

:::

<QuizChoice
title="if のどちらへ"
options={[
{id: 'a', label: 'bulk と出る'},
{id: 'b', label: 'normal と出る'},
{id: 'c', label: '何も出ない'},
]}
answer="a"
explanation="yen は 1200 なので yen &gt;= 1000 は true。if 側の塊が動きます。"

>

  <div>次のコードの出力はどうなりますか。</div>
  <pre className="language-java"><code>{`int yen = 1200;
if (yen >= 1000) {
    System.out.println("bulk");
} else {
    System.out.println("normal");
}`}</code></pre>
</QuizChoice>

## `else if`: メニュー表のように枝を増やす

ふたつでは足りないとき、`else if` で枝を増やします。  
サイズ選びのメニュー表に近いです。

```java
int yen = 500;

if (yen >= 1000) {
    System.out.println("bulk");
} else if (yen >= 100) {
    System.out.println("normal");
} else {
    System.out.println("small");
}
```

上から順に調べ、**最初に当てはまった枝だけ** が動きます。  
`500` は 1000 以上ではないが 100 以上なので、`"normal"` です。

順番を間違えると、意図と違う枝に入ります。  
厳しい条件（大きい金額など）を先に書くことが多いです。

## 条件を組み合わせる

複数の条件を同時に見たいときは、次を使います。

| 書き方     | 意味（ざっくり）     |
| ---------- | -------------------- |
| `a && b`   | 両方はいならはい     |
| `a \|\| b` | どちらかはいならはい |
| `!a`       | はい／いいえを反転   |

```java
int yen = 1200;
boolean member = true;

if (yen >= 1000 && member) {
    System.out.println("member-bulk");
}

if (yen < 0 || yen > 1_000_000) {
    System.out.println("out-of-range");
}

boolean closed = false;
if (!closed) {
    System.out.println("accepting");
}
```

まずは「かつ」「または」「ではない」が読めれば足ります。  
複雑な条件は、名前付きの `boolean` に一度入れてから `if` すると追いやすいです。

```java
boolean bulkMember = yen >= 1000 && member;
if (bulkMember) {
    System.out.println("member-bulk");
}
```

<details>
<summary>波括弧を省略する書き方</summary>

文が 1 つだけのとき、波括弧を省略できる構文もあります。  
読み違えやすいので、入門では **いつも `{ }` を付ける** 方が安全です。

</details>

## よくあるつまずき

- `=`（代入）を条件に書いてしまう（比較は `==`）
- `else if` の順番を逆にして、先の広い条件に吸われる
- `&&` と `||` を取り違える
- 波括弧の対応がズレて、見た目と違う道が動く

## まとめ / 次に学ぶとよいこと

- `if` / `else` で道を選ぶ。条件は `boolean`
- `else if` で枝を増やし、上から最初の当たりだけが動く
- `&&` / `||` / `!` で条件を組み合わせられる

次は [繰り返し](./loops) で、同じ作業を回数や条件に応じて回します。
