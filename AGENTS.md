# AGENTS.md

このリポジトリで作業するエージェント向けの指針です。ルールの本体はこのファイルに置き、`.cursor/rules` からは本ファイルを参照させます。

## プロジェクト概要

- **Training**: IT 技術の学習教材サイト（Docusaurus）
- **想定読者**: システムエンジニアとして新人レベルの読者（入社直後の案内ではなく、基礎から実務につながる説明）
- **コンテンツ配置**: `docs/` 配下（カテゴリ別ディレクトリ）
- **サイト設定**: `docusaurus.config.ts`、`sidebars.ts`（autogenerated）

## 教材カテゴリ

| ディレクトリ         | カテゴリ                                                             |
| -------------------- | -------------------------------------------------------------------- |
| `docs/foundations/`  | 共通基盤（Linux / Shell、Git、リモートアクセス、ネットワーク、HTTP） |
| `docs/languages/`    | 言語（Java、JavaScript / TypeScript、Rust）                          |
| `docs/data/`         | データ（SQL / RDB、NoSQL、メッセージング）                           |
| `docs/cloud/`        | クラウド（GCP、AWS、Azure）                                          |
| `docs/platform/`     | DevOps / コンテナ（Docker、Kubernetes、OpenShift、CI/CD、IaC）       |
| `docs/security/`     | セキュリティ（認証・認可、クラウドセキュリティ、OWASP）              |
| `docs/ai/`           | AI（LLM、Prompt / RAG、エージェント、MLOps）                         |
| `docs/architecture/` | 設計 / アーキテクチャ（API 設計、マイクロサービス、テスト）          |

新規トピックは既存カテゴリに配置する。  
どのカテゴリにも当てはまらない場合のみ、ユーザーに確認してから新カテゴリを提案する。  
無理に既存カテゴリに当てはめずに適切に提案を行うこと。

## 読者・トーン

- 仲の良い先輩社員が親身に教える語調で書く。堅苦しい論文調やマニュアル調は避ける。
- 読者は新人レベルとして書くが、入社直後の案内にはしない。「入社してすぐ」など、本当の新人向けの状況設定は使わない。
- 導入では現場でありがちな失敗や「あるある」に寄り添い、レトリックを利かせて引きつける。読者の実体験に接続してから概念へ入る。同じエピソードをページをまたいで使い回さない。
- わかりやすさのために、比喩・擬人・倒置・反復・誇張・対句・逆説などを **必要な場所だけ** 使ってよい。わかりやすさが落ちるなら使わない。装飾のためのレトリックは置かない。
- 上から目線や過度なカジュアルさは避ける。丁寧で実務的な日本語にする。
- 「簡単」「当たり前」など、初心者を萎縮させる表現は使わない。
- 概念の説明だけで終わらせない。具体例を使い、「何がうれしいか」「何を目的としたものか」「どんな課題を解決できるか」まで書く。
- 専門用語は使ってよい。ただし濫用して読者を置いてけぼりにしない。理解しておくべき用語は説明したうえで使い、必要なら平易な言い換えも添える。
- 「なぜ必要か」→「何か」→「どう使うか」の順で書く。
- 断定しすぎず、現場でよくある注意点や失敗例も短く添える。
- 同じ話を何度も書かない。既出の内容は参照リンクで飛ばす。ただし、復習としてあえて繰り返す説明は許可する。
- **まだ説明していない概念の半分だけを匂わせない。** 読者が判断材料を持たない予告（「実は変数に入っていない」「詳細は後で」など）は混乱の元になる。未出の話は出さないか、その場で完結させる。章末の「次は〜」はロードマップとして書いてよい。
- ストーリーラインと理解のロードマップを意識し、連続した理解が積み上がるように書く。話が飛ぶ場合は無理につなげず、別テーマとして分離する。
- 説明が長くなりすぎる部分や、本筋からそれる補足は `<details>` / `<summary>` で折りたたみ、読む導線の邪魔をしない。本筋に必要な一文は折りたたみの外に残す。
- `<details>` に向くもの: オプションの詳細、向き不向きの一覧、用語集、稀なエラー、旧コマンドとの対応、本筋を追うのに必須でない補足。  
  向かないもの: そのページの結論、次の手順に進むための必須条件、危険操作の注意そのもの（注意は本文か `:::warning` に残す）。
- 地の文が長くなり要点が埋もれるところは、Docusaurus のアドモニションでキーメッセージを打ち出す。多用しすぎず、1 セクションに 0〜2 個を目安にする。
- アドモニション本文は短い結論だけにする（長文の置き場にしない）。使い分けの目安:
  - `:::tip` … 覚えておく手順・判断の核
  - `:::info` … 誤解しやすい点の整理（「壊れたのではない」など）
  - `:::warning` … 破棄・強制上書き・共有後の危険操作など、誤ると痛い注意
- タイトル付きは **必ず** `:::tip[タイトル]` 形式にする。`:::tip タイトル`（スペース区切り）は、このリポジトリの `future.v4: true` では解釈されず文字どおり表示される。追加パッケージは不要（Docusaurus 標準）。

```md
:::tip[覚えておくこと]

ここに短いキーメッセージを書く。

:::
```

## コンテンツの書き方（完成形のみ）

ドキュメント本文には、次のようなメタ情報を書かない。

- 作成方針・執筆意図の説明
- 「指示を受けて修正した」などの経緯
- TODO、WIP、仮置き、今後書く予定の宣言（章立て一覧はカテゴリ index のみ可）
- エージェントやレビュー向けのメモ

読者がそのまま学習できる完成形の文章だけを書く。

## 例示のルール

- 例は一般的な用語・架空のサービス名で説明する。
- 特定企業の社内システム、非公開プロダクト、社固有の用語は使わない。
- 次のような広く知られたサービス・コンテンツの例示は許可する: Google、X、GitHub、AWS、GCP、Azure、Wikipedia など。
- コード例の識別子は `user`、`order`、`api` など汎用的な名前にする。
- 教材内で初めて出てくるコマンドには、代表的な **成功時** の実行結果の例を添え、出力のどこを見ればよいか（読み方）を説明する。2 回目以降は必要に応じて省略し、初出ページへの参照で足りる。
- 失敗例・稀なエラーだけを主例にしない。成功の流れを本文に置き、失敗時の表示は `<details>` や「よくあるつまずき」へ回す。
- 関係や流れの理解に図が有効なときだけ Mermaid を使う（コードフェンスの言語に `mermaid` を指定）。装飾や言い換えのための図は置かない。同じ内容を文章と図で冗長に繰り返さない。
- Mermaid の `gitGraph` では、ブランチ ID に `/` など区切り文字を使わない（例: `feature/login` ではなく `featureLogin`）。表示用の実名は図の直後の一文で補う。

## ドキュメント構成

各ページは次の流れを基本とする（トピックに応じて省略可）。

1. このページで分かること（2〜4 行）
2. 背景・なぜ学ぶか
3. 基本概念
4. 手順または具体例（コマンド・コード）
5. よくあるつまずき
6. まとめ / 次に学ぶとよいこと（章のロードマップ上の次ページへつなぐ）

### ファイル規約

- 本文は Markdown / MDX（`.mdx` を優先）
- 日本語見出し。ファイル名は英小文字の kebab-case に、同階層内の順序用プレフィクスを付ける（例: `000_http-basics.mdx`）
- プレフィクスは 3 桁数字 + `_`。下一桁は初期を 0 とし、十の位から 10 刻みで連番を付ける（`000_` → `010_` → `020_` …）。途中挿入は `015_` のように下一桁を使う
- カテゴリの入口である `index.mdx` には連番プレフィクスを付けない（付けるとカテゴリ URL が解決されなくなる）
- front matter の `id` には連番プレフィクスを付けない（連番の付け替えでリンク切れしないようにする）。Docusaurus は既定でファイル名の数字プレフィクスを ID / URL から除去する
- カテゴリ直下の `index.mdx` はその分野の概要と章への案内
- 新規カテゴリを追加する場合は `_category_.json`（`label` / `position`）も用意する
- 同階層の並びはファイル名の連番プレフィクスで制御する。必要なら front matter の `sidebar_position` も併用してよい
- 句点（`。`）の直後は半角スペース 2 つを入れて改行する（Markdown のソフト改行）。箇条書き・番号付きリストの項目内では適用しない
- 折りたたみは次の形式にする（`<details>` と `<summary>` のあいだに空行を入れない）

```html
<details>
  <summary>タイトル</summary>

  コンテンツ
</details>
```

### 理解チェック（クイズ）

- 確認クイズ専用の章は置かない。各ページの説明の区切りや末尾に、理解確認を短く挟む
- 実装は `QuizChoice`（選択式）と `QuizFill`（穴埋め）を使う。`src/theme/MDXComponents.tsx` でグローバル登録済みなので、MDX での import は不要
- サーバ送信はしない（ブラウザ内だけで正誤判定）。ソースを見れば正解が分かることは許容する
- 問題パネル（青緑系）と回答・解説パネル（琥珀色系）は別色の折りたたみになる。回答・解説パネルは「回答する」押下後にだけ表示する
- **理解チェックは、説明の塊ごとに挟む。** 「一つ教えて → 確認 → 次へ」を基本とし、章末だけにまとめない
  - 目安は見出し 1〜2 個に 1 問。厚いページでもおおむね 3〜5 問まで
  - 同じ論点を章末で重ねて聞かない（途中で確認済みなら末尾の重複は置かない）
- 正答は一意に定まる形にする。日本語の自由記述（「一言で言うと何か」）はブレて判定が不安定なので使わない
  - 概念を問う → `QuizChoice`（選択肢）
  - 穴埋め → コード中の識別子・拡張子・リテラルなど、表記がほぼ一通りのものに限る（例: `class`、`equals`）
  - 望ましいのは「このコードの実行結果はどれか」「空欄に入るトークンは何か」
  - 問題文は状況を長く説明せず、**確認したいコードをそのまま見せる**。理解対象以外の文章で認知負荷を増やさない
- クイズのコード断片は **その場で自己完結** させる。正答に必要な型・フィールド・メソッドはスニペット内に書き、本文の別例や未出の API（例: 触れていない `size()`）を想像させない。標準ライブラリの API は既出・常識の範囲で使ってよい
- 1 ページにクイズを詰め込みすぎて、本筋の「なぜ→何か→どう使うか」が読めなくならないようにする
- JSX の文字列属性の中に `<Type>` のような山括弧を直接書かない（MDX がタグと解釈する）。本文の `<code>` 内か、言い換えで表現する
- 問題文の子要素は `<p>` ではなく `<div>` を使う（MDX が外側で段落を付けるため、入れ子の `</p>` 警告になる）

```mdx
<QuizChoice
  title="短い題名"
  options={[
    { id: 'a', label: '選択肢 A' },
    { id: 'b', label: '選択肢 B' },
  ]}
  answer="b"
  explanation="なぜそうなるかの一文。"
>
  <div>問題文。必要ならここに補足を書く。</div>
</QuizChoice>
```

## 正確性・安全性

- 事実と慣習を混同しない。バージョン依存の話は対象バージョンを書く。
- セキュリティ、権限、本番操作に関する説明は、安全な手順を優先する。
- 破壊的操作（削除、強制 push、本番への直接変更など）は、リスクを明記したうえで書く。
- 秘密情報（API キー、パスワード、トークン）をサンプルに含めない。プレースホルダを使う。

## サイト実装のルール

- ブログは無効（`blog: false`）。復活させない。
- ブランド名・ロゴは既存の Training アセット（`static/img/training.png` 由来）を使う。
- ナビ・フッター・色・フォントを変えるときは、既存デザインとの一貫性を保つ。
- `baseUrl` は `/training/`。リンクや静的アセットは Docusaurus の仕組み（`Link`、`useBaseUrl` など）に従う。
- 不要な依存関係や設定の追加は最小限にする。

## 作業時の確認

教材やサイトを変更したら、必要に応じて次を行う。

```bash
npm start    # ローカル確認
npm run build
```

リンク切れやビルドエラーを残さない。

## やってはいけないこと

- プロジェクトのルール本体を `.cursor/` に置く・複製すること（`.cursor` は `AGENTS.md` への参照のみ）
- 読者向け本文に作業メモや修正経緯を残すこと
- ブログ機能の再追加
- 読者レベルを無視した高度な前提だけの説明
- 社内限定・特定顧客システム前提の例
